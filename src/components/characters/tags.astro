---
interface Props {
  tags: string[];
}
const { tags } = Astro.props;
---

<div class="flex flex-wrap gap-2 py-4">
  {
    tags.map((tag) => (
      <div class="rounded-full border-[1px] border-text py-1 px-2 !text-text">
        {tag}
      </div>
    ))
  }
</div>
